/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { FINDARE_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const CLAIM_FOUND_LISTING_DISCRIMINATOR = new Uint8Array([
  244, 92, 103, 22, 203, 232, 185, 90,
]);

export function getClaimFoundListingDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLAIM_FOUND_LISTING_DISCRIMINATOR
  );
}

export type ClaimFoundListingInstruction<
  TProgram extends string = typeof FINDARE_PROGRAM_ADDRESS,
  TAccountClaimer extends string | AccountMeta<string> = string,
  TAccountFoundPost extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountClaimTicket extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountClaimer extends string
        ? WritableSignerAccount<TAccountClaimer> &
            AccountSignerMeta<TAccountClaimer>
        : TAccountClaimer,
      TAccountFoundPost extends string
        ? WritableAccount<TAccountFoundPost>
        : TAccountFoundPost,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountClaimTicket extends string
        ? WritableAccount<TAccountClaimTicket>
        : TAccountClaimTicket,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ClaimFoundListingInstructionData = {
  discriminator: ReadonlyUint8Array;
  claimNotes: string;
  claimDeposit: bigint;
};

export type ClaimFoundListingInstructionDataArgs = {
  claimNotes: string;
  claimDeposit: number | bigint;
};

export function getClaimFoundListingInstructionDataEncoder(): Encoder<ClaimFoundListingInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['claimNotes', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['claimDeposit', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CLAIM_FOUND_LISTING_DISCRIMINATOR })
  );
}

export function getClaimFoundListingInstructionDataDecoder(): Decoder<ClaimFoundListingInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['claimNotes', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['claimDeposit', getU64Decoder()],
  ]);
}

export function getClaimFoundListingInstructionDataCodec(): Codec<
  ClaimFoundListingInstructionDataArgs,
  ClaimFoundListingInstructionData
> {
  return combineCodec(
    getClaimFoundListingInstructionDataEncoder(),
    getClaimFoundListingInstructionDataDecoder()
  );
}

export type ClaimFoundListingAsyncInput<
  TAccountClaimer extends string = string,
  TAccountFoundPost extends string = string,
  TAccountConfig extends string = string,
  TAccountClaimTicket extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  claimer: TransactionSigner<TAccountClaimer>;
  foundPost: Address<TAccountFoundPost>;
  config: Address<TAccountConfig>;
  claimTicket?: Address<TAccountClaimTicket>;
  systemProgram?: Address<TAccountSystemProgram>;
  claimNotes: ClaimFoundListingInstructionDataArgs['claimNotes'];
  claimDeposit: ClaimFoundListingInstructionDataArgs['claimDeposit'];
};

export async function getClaimFoundListingInstructionAsync<
  TAccountClaimer extends string,
  TAccountFoundPost extends string,
  TAccountConfig extends string,
  TAccountClaimTicket extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FINDARE_PROGRAM_ADDRESS,
>(
  input: ClaimFoundListingAsyncInput<
    TAccountClaimer,
    TAccountFoundPost,
    TAccountConfig,
    TAccountClaimTicket,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  ClaimFoundListingInstruction<
    TProgramAddress,
    TAccountClaimer,
    TAccountFoundPost,
    TAccountConfig,
    TAccountClaimTicket,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? FINDARE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    claimer: { value: input.claimer ?? null, isWritable: true },
    foundPost: { value: input.foundPost ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    claimTicket: { value: input.claimTicket ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.claimTicket.value) {
    accounts.claimTicket.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            99, 108, 97, 105, 109, 45, 116, 105, 99, 107, 101, 116,
          ])
        ),
        getAddressEncoder().encode(expectAddress(accounts.foundPost.value)),
        getAddressEncoder().encode(expectAddress(accounts.claimer.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.claimer),
      getAccountMeta(accounts.foundPost),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.claimTicket),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getClaimFoundListingInstructionDataEncoder().encode(
      args as ClaimFoundListingInstructionDataArgs
    ),
    programAddress,
  } as ClaimFoundListingInstruction<
    TProgramAddress,
    TAccountClaimer,
    TAccountFoundPost,
    TAccountConfig,
    TAccountClaimTicket,
    TAccountSystemProgram
  >);
}

export type ClaimFoundListingInput<
  TAccountClaimer extends string = string,
  TAccountFoundPost extends string = string,
  TAccountConfig extends string = string,
  TAccountClaimTicket extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  claimer: TransactionSigner<TAccountClaimer>;
  foundPost: Address<TAccountFoundPost>;
  config: Address<TAccountConfig>;
  claimTicket: Address<TAccountClaimTicket>;
  systemProgram?: Address<TAccountSystemProgram>;
  claimNotes: ClaimFoundListingInstructionDataArgs['claimNotes'];
  claimDeposit: ClaimFoundListingInstructionDataArgs['claimDeposit'];
};

export function getClaimFoundListingInstruction<
  TAccountClaimer extends string,
  TAccountFoundPost extends string,
  TAccountConfig extends string,
  TAccountClaimTicket extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FINDARE_PROGRAM_ADDRESS,
>(
  input: ClaimFoundListingInput<
    TAccountClaimer,
    TAccountFoundPost,
    TAccountConfig,
    TAccountClaimTicket,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): ClaimFoundListingInstruction<
  TProgramAddress,
  TAccountClaimer,
  TAccountFoundPost,
  TAccountConfig,
  TAccountClaimTicket,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? FINDARE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    claimer: { value: input.claimer ?? null, isWritable: true },
    foundPost: { value: input.foundPost ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    claimTicket: { value: input.claimTicket ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.claimer),
      getAccountMeta(accounts.foundPost),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.claimTicket),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getClaimFoundListingInstructionDataEncoder().encode(
      args as ClaimFoundListingInstructionDataArgs
    ),
    programAddress,
  } as ClaimFoundListingInstruction<
    TProgramAddress,
    TAccountClaimer,
    TAccountFoundPost,
    TAccountConfig,
    TAccountClaimTicket,
    TAccountSystemProgram
  >);
}

export type ParsedClaimFoundListingInstruction<
  TProgram extends string = typeof FINDARE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    claimer: TAccountMetas[0];
    foundPost: TAccountMetas[1];
    config: TAccountMetas[2];
    claimTicket: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: ClaimFoundListingInstructionData;
};

export function parseClaimFoundListingInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedClaimFoundListingInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      claimer: getNextAccount(),
      foundPost: getNextAccount(),
      config: getNextAccount(),
      claimTicket: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getClaimFoundListingInstructionDataDecoder().decode(instruction.data),
  };
}
