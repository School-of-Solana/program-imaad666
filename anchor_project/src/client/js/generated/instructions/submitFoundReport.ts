/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { FINDARE_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const SUBMIT_FOUND_REPORT_DISCRIMINATOR = new Uint8Array([
  35, 120, 228, 120, 148, 120, 221, 169,
]);

export function getSubmitFoundReportDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SUBMIT_FOUND_REPORT_DISCRIMINATOR
  );
}

export type SubmitFoundReportInstruction<
  TProgram extends string = typeof FINDARE_PROGRAM_ADDRESS,
  TAccountFinder extends string | AccountMeta<string> = string,
  TAccountLostPost extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountFoundReport extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountFinder extends string
        ? WritableSignerAccount<TAccountFinder> &
            AccountSignerMeta<TAccountFinder>
        : TAccountFinder,
      TAccountLostPost extends string
        ? WritableAccount<TAccountLostPost>
        : TAccountLostPost,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountFoundReport extends string
        ? WritableAccount<TAccountFoundReport>
        : TAccountFoundReport,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SubmitFoundReportInstructionData = {
  discriminator: ReadonlyUint8Array;
  evidenceUri: string;
};

export type SubmitFoundReportInstructionDataArgs = { evidenceUri: string };

export function getSubmitFoundReportInstructionDataEncoder(): Encoder<SubmitFoundReportInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['evidenceUri', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: SUBMIT_FOUND_REPORT_DISCRIMINATOR })
  );
}

export function getSubmitFoundReportInstructionDataDecoder(): Decoder<SubmitFoundReportInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['evidenceUri', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getSubmitFoundReportInstructionDataCodec(): Codec<
  SubmitFoundReportInstructionDataArgs,
  SubmitFoundReportInstructionData
> {
  return combineCodec(
    getSubmitFoundReportInstructionDataEncoder(),
    getSubmitFoundReportInstructionDataDecoder()
  );
}

export type SubmitFoundReportAsyncInput<
  TAccountFinder extends string = string,
  TAccountLostPost extends string = string,
  TAccountConfig extends string = string,
  TAccountFoundReport extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  finder: TransactionSigner<TAccountFinder>;
  lostPost: Address<TAccountLostPost>;
  config: Address<TAccountConfig>;
  foundReport?: Address<TAccountFoundReport>;
  systemProgram?: Address<TAccountSystemProgram>;
  evidenceUri: SubmitFoundReportInstructionDataArgs['evidenceUri'];
};

export async function getSubmitFoundReportInstructionAsync<
  TAccountFinder extends string,
  TAccountLostPost extends string,
  TAccountConfig extends string,
  TAccountFoundReport extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FINDARE_PROGRAM_ADDRESS,
>(
  input: SubmitFoundReportAsyncInput<
    TAccountFinder,
    TAccountLostPost,
    TAccountConfig,
    TAccountFoundReport,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  SubmitFoundReportInstruction<
    TProgramAddress,
    TAccountFinder,
    TAccountLostPost,
    TAccountConfig,
    TAccountFoundReport,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? FINDARE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    finder: { value: input.finder ?? null, isWritable: true },
    lostPost: { value: input.lostPost ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    foundReport: { value: input.foundReport ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.foundReport.value) {
    accounts.foundReport.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            102, 111, 117, 110, 100, 45, 114, 101, 112, 111, 114, 116,
          ])
        ),
        getAddressEncoder().encode(expectAddress(accounts.lostPost.value)),
        getAddressEncoder().encode(expectAddress(accounts.finder.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.finder),
      getAccountMeta(accounts.lostPost),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.foundReport),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getSubmitFoundReportInstructionDataEncoder().encode(
      args as SubmitFoundReportInstructionDataArgs
    ),
    programAddress,
  } as SubmitFoundReportInstruction<
    TProgramAddress,
    TAccountFinder,
    TAccountLostPost,
    TAccountConfig,
    TAccountFoundReport,
    TAccountSystemProgram
  >);
}

export type SubmitFoundReportInput<
  TAccountFinder extends string = string,
  TAccountLostPost extends string = string,
  TAccountConfig extends string = string,
  TAccountFoundReport extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  finder: TransactionSigner<TAccountFinder>;
  lostPost: Address<TAccountLostPost>;
  config: Address<TAccountConfig>;
  foundReport: Address<TAccountFoundReport>;
  systemProgram?: Address<TAccountSystemProgram>;
  evidenceUri: SubmitFoundReportInstructionDataArgs['evidenceUri'];
};

export function getSubmitFoundReportInstruction<
  TAccountFinder extends string,
  TAccountLostPost extends string,
  TAccountConfig extends string,
  TAccountFoundReport extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FINDARE_PROGRAM_ADDRESS,
>(
  input: SubmitFoundReportInput<
    TAccountFinder,
    TAccountLostPost,
    TAccountConfig,
    TAccountFoundReport,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): SubmitFoundReportInstruction<
  TProgramAddress,
  TAccountFinder,
  TAccountLostPost,
  TAccountConfig,
  TAccountFoundReport,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? FINDARE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    finder: { value: input.finder ?? null, isWritable: true },
    lostPost: { value: input.lostPost ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    foundReport: { value: input.foundReport ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.finder),
      getAccountMeta(accounts.lostPost),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.foundReport),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getSubmitFoundReportInstructionDataEncoder().encode(
      args as SubmitFoundReportInstructionDataArgs
    ),
    programAddress,
  } as SubmitFoundReportInstruction<
    TProgramAddress,
    TAccountFinder,
    TAccountLostPost,
    TAccountConfig,
    TAccountFoundReport,
    TAccountSystemProgram
  >);
}

export type ParsedSubmitFoundReportInstruction<
  TProgram extends string = typeof FINDARE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    finder: TAccountMetas[0];
    lostPost: TAccountMetas[1];
    config: TAccountMetas[2];
    foundReport: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: SubmitFoundReportInstructionData;
};

export function parseSubmitFoundReportInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedSubmitFoundReportInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      finder: getNextAccount(),
      lostPost: getNextAccount(),
      config: getNextAccount(),
      foundReport: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getSubmitFoundReportInstructionDataDecoder().decode(instruction.data),
  };
}
