/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { FINDARE_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const CLOSE_LOST_POST_DISCRIMINATOR = new Uint8Array([
  43, 200, 112, 80, 242, 142, 25, 9,
]);

export function getCloseLostPostDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_LOST_POST_DISCRIMINATOR
  );
}

export type CloseLostPostInstruction<
  TProgram extends string = typeof FINDARE_PROGRAM_ADDRESS,
  TAccountLostPost extends string | AccountMeta<string> = string,
  TAccountPoster extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountLostPost extends string
        ? WritableAccount<TAccountLostPost>
        : TAccountLostPost,
      TAccountPoster extends string
        ? WritableSignerAccount<TAccountPoster> &
            AccountSignerMeta<TAccountPoster>
        : TAccountPoster,
      ...TRemainingAccounts,
    ]
  >;

export type CloseLostPostInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CloseLostPostInstructionDataArgs = {};

export function getCloseLostPostInstructionDataEncoder(): FixedSizeEncoder<CloseLostPostInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLOSE_LOST_POST_DISCRIMINATOR })
  );
}

export function getCloseLostPostInstructionDataDecoder(): FixedSizeDecoder<CloseLostPostInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCloseLostPostInstructionDataCodec(): FixedSizeCodec<
  CloseLostPostInstructionDataArgs,
  CloseLostPostInstructionData
> {
  return combineCodec(
    getCloseLostPostInstructionDataEncoder(),
    getCloseLostPostInstructionDataDecoder()
  );
}

export type CloseLostPostInput<
  TAccountLostPost extends string = string,
  TAccountPoster extends string = string,
> = {
  lostPost: Address<TAccountLostPost>;
  poster: TransactionSigner<TAccountPoster>;
};

export function getCloseLostPostInstruction<
  TAccountLostPost extends string,
  TAccountPoster extends string,
  TProgramAddress extends Address = typeof FINDARE_PROGRAM_ADDRESS,
>(
  input: CloseLostPostInput<TAccountLostPost, TAccountPoster>,
  config?: { programAddress?: TProgramAddress }
): CloseLostPostInstruction<TProgramAddress, TAccountLostPost, TAccountPoster> {
  // Program address.
  const programAddress = config?.programAddress ?? FINDARE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    lostPost: { value: input.lostPost ?? null, isWritable: true },
    poster: { value: input.poster ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.lostPost),
      getAccountMeta(accounts.poster),
    ],
    data: getCloseLostPostInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseLostPostInstruction<
    TProgramAddress,
    TAccountLostPost,
    TAccountPoster
  >);
}

export type ParsedCloseLostPostInstruction<
  TProgram extends string = typeof FINDARE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    lostPost: TAccountMetas[0];
    poster: TAccountMetas[1];
  };
  data: CloseLostPostInstructionData;
};

export function parseCloseLostPostInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCloseLostPostInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { lostPost: getNextAccount(), poster: getNextAccount() },
    data: getCloseLostPostInstructionDataDecoder().decode(instruction.data),
  };
}
