/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { FINDARE_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const CREATE_FOUND_LISTING_DISCRIMINATOR = new Uint8Array([
  64, 56, 172, 124, 253, 31, 73, 79,
]);

export function getCreateFoundListingDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_FOUND_LISTING_DISCRIMINATOR
  );
}

export type CreateFoundListingInstruction<
  TProgram extends string = typeof FINDARE_PROGRAM_ADDRESS,
  TAccountFinder extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountFoundPost extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountFinder extends string
        ? WritableSignerAccount<TAccountFinder> &
            AccountSignerMeta<TAccountFinder>
        : TAccountFinder,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountFoundPost extends string
        ? WritableAccount<TAccountFoundPost>
        : TAccountFoundPost,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateFoundListingInstructionData = {
  discriminator: ReadonlyUint8Array;
  postId: bigint;
  title: string;
  description: string;
  attributes: string;
  photoRef: string;
};

export type CreateFoundListingInstructionDataArgs = {
  postId: number | bigint;
  title: string;
  description: string;
  attributes: string;
  photoRef: string;
};

export function getCreateFoundListingInstructionDataEncoder(): Encoder<CreateFoundListingInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['postId', getU64Encoder()],
      ['title', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['description', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['attributes', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['photoRef', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: CREATE_FOUND_LISTING_DISCRIMINATOR })
  );
}

export function getCreateFoundListingInstructionDataDecoder(): Decoder<CreateFoundListingInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['postId', getU64Decoder()],
    ['title', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['description', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['attributes', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['photoRef', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getCreateFoundListingInstructionDataCodec(): Codec<
  CreateFoundListingInstructionDataArgs,
  CreateFoundListingInstructionData
> {
  return combineCodec(
    getCreateFoundListingInstructionDataEncoder(),
    getCreateFoundListingInstructionDataDecoder()
  );
}

export type CreateFoundListingAsyncInput<
  TAccountFinder extends string = string,
  TAccountConfig extends string = string,
  TAccountFoundPost extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  finder: TransactionSigner<TAccountFinder>;
  config: Address<TAccountConfig>;
  foundPost?: Address<TAccountFoundPost>;
  systemProgram?: Address<TAccountSystemProgram>;
  postId: CreateFoundListingInstructionDataArgs['postId'];
  title: CreateFoundListingInstructionDataArgs['title'];
  description: CreateFoundListingInstructionDataArgs['description'];
  attributes: CreateFoundListingInstructionDataArgs['attributes'];
  photoRef: CreateFoundListingInstructionDataArgs['photoRef'];
};

export async function getCreateFoundListingInstructionAsync<
  TAccountFinder extends string,
  TAccountConfig extends string,
  TAccountFoundPost extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FINDARE_PROGRAM_ADDRESS,
>(
  input: CreateFoundListingAsyncInput<
    TAccountFinder,
    TAccountConfig,
    TAccountFoundPost,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  CreateFoundListingInstruction<
    TProgramAddress,
    TAccountFinder,
    TAccountConfig,
    TAccountFoundPost,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? FINDARE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    finder: { value: input.finder ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    foundPost: { value: input.foundPost ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.foundPost.value) {
    accounts.foundPost.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([102, 111, 117, 110, 100, 45, 112, 111, 115, 116])
        ),
        getAddressEncoder().encode(expectAddress(accounts.finder.value)),
        getU64Encoder().encode(expectSome(args.postId)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.finder),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.foundPost),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateFoundListingInstructionDataEncoder().encode(
      args as CreateFoundListingInstructionDataArgs
    ),
    programAddress,
  } as CreateFoundListingInstruction<
    TProgramAddress,
    TAccountFinder,
    TAccountConfig,
    TAccountFoundPost,
    TAccountSystemProgram
  >);
}

export type CreateFoundListingInput<
  TAccountFinder extends string = string,
  TAccountConfig extends string = string,
  TAccountFoundPost extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  finder: TransactionSigner<TAccountFinder>;
  config: Address<TAccountConfig>;
  foundPost: Address<TAccountFoundPost>;
  systemProgram?: Address<TAccountSystemProgram>;
  postId: CreateFoundListingInstructionDataArgs['postId'];
  title: CreateFoundListingInstructionDataArgs['title'];
  description: CreateFoundListingInstructionDataArgs['description'];
  attributes: CreateFoundListingInstructionDataArgs['attributes'];
  photoRef: CreateFoundListingInstructionDataArgs['photoRef'];
};

export function getCreateFoundListingInstruction<
  TAccountFinder extends string,
  TAccountConfig extends string,
  TAccountFoundPost extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FINDARE_PROGRAM_ADDRESS,
>(
  input: CreateFoundListingInput<
    TAccountFinder,
    TAccountConfig,
    TAccountFoundPost,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): CreateFoundListingInstruction<
  TProgramAddress,
  TAccountFinder,
  TAccountConfig,
  TAccountFoundPost,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? FINDARE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    finder: { value: input.finder ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    foundPost: { value: input.foundPost ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.finder),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.foundPost),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateFoundListingInstructionDataEncoder().encode(
      args as CreateFoundListingInstructionDataArgs
    ),
    programAddress,
  } as CreateFoundListingInstruction<
    TProgramAddress,
    TAccountFinder,
    TAccountConfig,
    TAccountFoundPost,
    TAccountSystemProgram
  >);
}

export type ParsedCreateFoundListingInstruction<
  TProgram extends string = typeof FINDARE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    finder: TAccountMetas[0];
    config: TAccountMetas[1];
    foundPost: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: CreateFoundListingInstructionData;
};

export function parseCreateFoundListingInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCreateFoundListingInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      finder: getNextAccount(),
      config: getNextAccount(),
      foundPost: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateFoundListingInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
