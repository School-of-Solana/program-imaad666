/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from 'gill';
import { FINDARE_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const REJECT_CLAIM_DISCRIMINATOR = new Uint8Array([
  238, 185, 227, 8, 51, 188, 35, 182,
]);

export function getRejectClaimDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REJECT_CLAIM_DISCRIMINATOR
  );
}

export type RejectClaimInstruction<
  TProgram extends string = typeof FINDARE_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountFoundPost extends string | AccountMeta<string> = string,
  TAccountClaimTicket extends string | AccountMeta<string> = string,
  TAccountClaimer extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountFoundPost extends string
        ? WritableAccount<TAccountFoundPost>
        : TAccountFoundPost,
      TAccountClaimTicket extends string
        ? WritableAccount<TAccountClaimTicket>
        : TAccountClaimTicket,
      TAccountClaimer extends string
        ? WritableAccount<TAccountClaimer>
        : TAccountClaimer,
      ...TRemainingAccounts,
    ]
  >;

export type RejectClaimInstructionData = { discriminator: ReadonlyUint8Array };

export type RejectClaimInstructionDataArgs = {};

export function getRejectClaimInstructionDataEncoder(): FixedSizeEncoder<RejectClaimInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: REJECT_CLAIM_DISCRIMINATOR })
  );
}

export function getRejectClaimInstructionDataDecoder(): FixedSizeDecoder<RejectClaimInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRejectClaimInstructionDataCodec(): FixedSizeCodec<
  RejectClaimInstructionDataArgs,
  RejectClaimInstructionData
> {
  return combineCodec(
    getRejectClaimInstructionDataEncoder(),
    getRejectClaimInstructionDataDecoder()
  );
}

export type RejectClaimInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountFoundPost extends string = string,
  TAccountClaimTicket extends string = string,
  TAccountClaimer extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config: Address<TAccountConfig>;
  foundPost: Address<TAccountFoundPost>;
  claimTicket: Address<TAccountClaimTicket>;
  claimer: Address<TAccountClaimer>;
};

export function getRejectClaimInstruction<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountFoundPost extends string,
  TAccountClaimTicket extends string,
  TAccountClaimer extends string,
  TProgramAddress extends Address = typeof FINDARE_PROGRAM_ADDRESS,
>(
  input: RejectClaimInput<
    TAccountAdmin,
    TAccountConfig,
    TAccountFoundPost,
    TAccountClaimTicket,
    TAccountClaimer
  >,
  config?: { programAddress?: TProgramAddress }
): RejectClaimInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountConfig,
  TAccountFoundPost,
  TAccountClaimTicket,
  TAccountClaimer
> {
  // Program address.
  const programAddress = config?.programAddress ?? FINDARE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    foundPost: { value: input.foundPost ?? null, isWritable: true },
    claimTicket: { value: input.claimTicket ?? null, isWritable: true },
    claimer: { value: input.claimer ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.foundPost),
      getAccountMeta(accounts.claimTicket),
      getAccountMeta(accounts.claimer),
    ],
    data: getRejectClaimInstructionDataEncoder().encode({}),
    programAddress,
  } as RejectClaimInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountFoundPost,
    TAccountClaimTicket,
    TAccountClaimer
  >);
}

export type ParsedRejectClaimInstruction<
  TProgram extends string = typeof FINDARE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    config: TAccountMetas[1];
    foundPost: TAccountMetas[2];
    claimTicket: TAccountMetas[3];
    claimer: TAccountMetas[4];
  };
  data: RejectClaimInstructionData;
};

export function parseRejectClaimInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedRejectClaimInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      config: getNextAccount(),
      foundPost: getNextAccount(),
      claimTicket: getNextAccount(),
      claimer: getNextAccount(),
    },
    data: getRejectClaimInstructionDataDecoder().decode(instruction.data),
  };
}
