/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { FINDARE_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const CREATE_LOST_POST_DISCRIMINATOR = new Uint8Array([
  87, 38, 95, 116, 43, 130, 252, 179,
]);

export function getCreateLostPostDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_LOST_POST_DISCRIMINATOR
  );
}

export type CreateLostPostInstruction<
  TProgram extends string = typeof FINDARE_PROGRAM_ADDRESS,
  TAccountPoster extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountLostPost extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPoster extends string
        ? WritableSignerAccount<TAccountPoster> &
            AccountSignerMeta<TAccountPoster>
        : TAccountPoster,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountLostPost extends string
        ? WritableAccount<TAccountLostPost>
        : TAccountLostPost,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateLostPostInstructionData = {
  discriminator: ReadonlyUint8Array;
  postId: bigint;
  title: string;
  description: string;
  attributes: string;
  photoRef: string;
  rewardLamports: bigint;
};

export type CreateLostPostInstructionDataArgs = {
  postId: number | bigint;
  title: string;
  description: string;
  attributes: string;
  photoRef: string;
  rewardLamports: number | bigint;
};

export function getCreateLostPostInstructionDataEncoder(): Encoder<CreateLostPostInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['postId', getU64Encoder()],
      ['title', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['description', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['attributes', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['photoRef', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ['rewardLamports', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CREATE_LOST_POST_DISCRIMINATOR })
  );
}

export function getCreateLostPostInstructionDataDecoder(): Decoder<CreateLostPostInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['postId', getU64Decoder()],
    ['title', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['description', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['attributes', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['photoRef', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ['rewardLamports', getU64Decoder()],
  ]);
}

export function getCreateLostPostInstructionDataCodec(): Codec<
  CreateLostPostInstructionDataArgs,
  CreateLostPostInstructionData
> {
  return combineCodec(
    getCreateLostPostInstructionDataEncoder(),
    getCreateLostPostInstructionDataDecoder()
  );
}

export type CreateLostPostAsyncInput<
  TAccountPoster extends string = string,
  TAccountConfig extends string = string,
  TAccountLostPost extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  poster: TransactionSigner<TAccountPoster>;
  config: Address<TAccountConfig>;
  lostPost?: Address<TAccountLostPost>;
  systemProgram?: Address<TAccountSystemProgram>;
  postId: CreateLostPostInstructionDataArgs['postId'];
  title: CreateLostPostInstructionDataArgs['title'];
  description: CreateLostPostInstructionDataArgs['description'];
  attributes: CreateLostPostInstructionDataArgs['attributes'];
  photoRef: CreateLostPostInstructionDataArgs['photoRef'];
  rewardLamports: CreateLostPostInstructionDataArgs['rewardLamports'];
};

export async function getCreateLostPostInstructionAsync<
  TAccountPoster extends string,
  TAccountConfig extends string,
  TAccountLostPost extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FINDARE_PROGRAM_ADDRESS,
>(
  input: CreateLostPostAsyncInput<
    TAccountPoster,
    TAccountConfig,
    TAccountLostPost,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  CreateLostPostInstruction<
    TProgramAddress,
    TAccountPoster,
    TAccountConfig,
    TAccountLostPost,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? FINDARE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    poster: { value: input.poster ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    lostPost: { value: input.lostPost ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.lostPost.value) {
    accounts.lostPost.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([108, 111, 115, 116, 45, 112, 111, 115, 116])
        ),
        getAddressEncoder().encode(expectAddress(accounts.poster.value)),
        getU64Encoder().encode(expectSome(args.postId)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.poster),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.lostPost),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateLostPostInstructionDataEncoder().encode(
      args as CreateLostPostInstructionDataArgs
    ),
    programAddress,
  } as CreateLostPostInstruction<
    TProgramAddress,
    TAccountPoster,
    TAccountConfig,
    TAccountLostPost,
    TAccountSystemProgram
  >);
}

export type CreateLostPostInput<
  TAccountPoster extends string = string,
  TAccountConfig extends string = string,
  TAccountLostPost extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  poster: TransactionSigner<TAccountPoster>;
  config: Address<TAccountConfig>;
  lostPost: Address<TAccountLostPost>;
  systemProgram?: Address<TAccountSystemProgram>;
  postId: CreateLostPostInstructionDataArgs['postId'];
  title: CreateLostPostInstructionDataArgs['title'];
  description: CreateLostPostInstructionDataArgs['description'];
  attributes: CreateLostPostInstructionDataArgs['attributes'];
  photoRef: CreateLostPostInstructionDataArgs['photoRef'];
  rewardLamports: CreateLostPostInstructionDataArgs['rewardLamports'];
};

export function getCreateLostPostInstruction<
  TAccountPoster extends string,
  TAccountConfig extends string,
  TAccountLostPost extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FINDARE_PROGRAM_ADDRESS,
>(
  input: CreateLostPostInput<
    TAccountPoster,
    TAccountConfig,
    TAccountLostPost,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): CreateLostPostInstruction<
  TProgramAddress,
  TAccountPoster,
  TAccountConfig,
  TAccountLostPost,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? FINDARE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    poster: { value: input.poster ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    lostPost: { value: input.lostPost ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.poster),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.lostPost),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateLostPostInstructionDataEncoder().encode(
      args as CreateLostPostInstructionDataArgs
    ),
    programAddress,
  } as CreateLostPostInstruction<
    TProgramAddress,
    TAccountPoster,
    TAccountConfig,
    TAccountLostPost,
    TAccountSystemProgram
  >);
}

export type ParsedCreateLostPostInstruction<
  TProgram extends string = typeof FINDARE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    poster: TAccountMetas[0];
    config: TAccountMetas[1];
    lostPost: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: CreateLostPostInstructionData;
};

export function parseCreateLostPostInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCreateLostPostInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      poster: getNextAccount(),
      config: getNextAccount(),
      lostPost: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateLostPostInstructionDataDecoder().decode(instruction.data),
  };
}
