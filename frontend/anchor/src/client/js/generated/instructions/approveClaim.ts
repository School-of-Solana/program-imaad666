/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from 'gill';
import { FINDARE_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const APPROVE_CLAIM_DISCRIMINATOR = new Uint8Array([
  74, 228, 211, 63, 140, 255, 69, 210,
]);

export function getApproveClaimDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    APPROVE_CLAIM_DISCRIMINATOR
  );
}

export type ApproveClaimInstruction<
  TProgram extends string = typeof FINDARE_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountFoundPost extends string | AccountMeta<string> = string,
  TAccountClaimTicket extends string | AccountMeta<string> = string,
  TAccountFinder extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountFoundPost extends string
        ? WritableAccount<TAccountFoundPost>
        : TAccountFoundPost,
      TAccountClaimTicket extends string
        ? WritableAccount<TAccountClaimTicket>
        : TAccountClaimTicket,
      TAccountFinder extends string
        ? WritableAccount<TAccountFinder>
        : TAccountFinder,
      ...TRemainingAccounts,
    ]
  >;

export type ApproveClaimInstructionData = { discriminator: ReadonlyUint8Array };

export type ApproveClaimInstructionDataArgs = {};

export function getApproveClaimInstructionDataEncoder(): FixedSizeEncoder<ApproveClaimInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: APPROVE_CLAIM_DISCRIMINATOR })
  );
}

export function getApproveClaimInstructionDataDecoder(): FixedSizeDecoder<ApproveClaimInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getApproveClaimInstructionDataCodec(): FixedSizeCodec<
  ApproveClaimInstructionDataArgs,
  ApproveClaimInstructionData
> {
  return combineCodec(
    getApproveClaimInstructionDataEncoder(),
    getApproveClaimInstructionDataDecoder()
  );
}

export type ApproveClaimInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountFoundPost extends string = string,
  TAccountClaimTicket extends string = string,
  TAccountFinder extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config: Address<TAccountConfig>;
  foundPost: Address<TAccountFoundPost>;
  claimTicket: Address<TAccountClaimTicket>;
  finder: Address<TAccountFinder>;
};

export function getApproveClaimInstruction<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountFoundPost extends string,
  TAccountClaimTicket extends string,
  TAccountFinder extends string,
  TProgramAddress extends Address = typeof FINDARE_PROGRAM_ADDRESS,
>(
  input: ApproveClaimInput<
    TAccountAdmin,
    TAccountConfig,
    TAccountFoundPost,
    TAccountClaimTicket,
    TAccountFinder
  >,
  config?: { programAddress?: TProgramAddress }
): ApproveClaimInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountConfig,
  TAccountFoundPost,
  TAccountClaimTicket,
  TAccountFinder
> {
  // Program address.
  const programAddress = config?.programAddress ?? FINDARE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    foundPost: { value: input.foundPost ?? null, isWritable: true },
    claimTicket: { value: input.claimTicket ?? null, isWritable: true },
    finder: { value: input.finder ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.foundPost),
      getAccountMeta(accounts.claimTicket),
      getAccountMeta(accounts.finder),
    ],
    data: getApproveClaimInstructionDataEncoder().encode({}),
    programAddress,
  } as ApproveClaimInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountFoundPost,
    TAccountClaimTicket,
    TAccountFinder
  >);
}

export type ParsedApproveClaimInstruction<
  TProgram extends string = typeof FINDARE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    config: TAccountMetas[1];
    foundPost: TAccountMetas[2];
    claimTicket: TAccountMetas[3];
    finder: TAccountMetas[4];
  };
  data: ApproveClaimInstructionData;
};

export function parseApproveClaimInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedApproveClaimInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      config: getNextAccount(),
      foundPost: getNextAccount(),
      claimTicket: getNextAccount(),
      finder: getNextAccount(),
    },
    data: getApproveClaimInstructionDataDecoder().decode(instruction.data),
  };
}
