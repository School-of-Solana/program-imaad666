/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from 'gill';
import { FINDARE_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const APPROVE_FOUND_REPORT_DISCRIMINATOR = new Uint8Array([
  125, 30, 145, 67, 188, 85, 146, 93,
]);

export function getApproveFoundReportDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    APPROVE_FOUND_REPORT_DISCRIMINATOR
  );
}

export type ApproveFoundReportInstruction<
  TProgram extends string = typeof FINDARE_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountLostPost extends string | AccountMeta<string> = string,
  TAccountFoundReport extends string | AccountMeta<string> = string,
  TAccountFinder extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountLostPost extends string
        ? WritableAccount<TAccountLostPost>
        : TAccountLostPost,
      TAccountFoundReport extends string
        ? WritableAccount<TAccountFoundReport>
        : TAccountFoundReport,
      TAccountFinder extends string
        ? WritableAccount<TAccountFinder>
        : TAccountFinder,
      ...TRemainingAccounts,
    ]
  >;

export type ApproveFoundReportInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ApproveFoundReportInstructionDataArgs = {};

export function getApproveFoundReportInstructionDataEncoder(): FixedSizeEncoder<ApproveFoundReportInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: APPROVE_FOUND_REPORT_DISCRIMINATOR })
  );
}

export function getApproveFoundReportInstructionDataDecoder(): FixedSizeDecoder<ApproveFoundReportInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getApproveFoundReportInstructionDataCodec(): FixedSizeCodec<
  ApproveFoundReportInstructionDataArgs,
  ApproveFoundReportInstructionData
> {
  return combineCodec(
    getApproveFoundReportInstructionDataEncoder(),
    getApproveFoundReportInstructionDataDecoder()
  );
}

export type ApproveFoundReportInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
  TAccountLostPost extends string = string,
  TAccountFoundReport extends string = string,
  TAccountFinder extends string = string,
> = {
  admin: TransactionSigner<TAccountAdmin>;
  config: Address<TAccountConfig>;
  lostPost: Address<TAccountLostPost>;
  foundReport: Address<TAccountFoundReport>;
  finder: Address<TAccountFinder>;
};

export function getApproveFoundReportInstruction<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TAccountLostPost extends string,
  TAccountFoundReport extends string,
  TAccountFinder extends string,
  TProgramAddress extends Address = typeof FINDARE_PROGRAM_ADDRESS,
>(
  input: ApproveFoundReportInput<
    TAccountAdmin,
    TAccountConfig,
    TAccountLostPost,
    TAccountFoundReport,
    TAccountFinder
  >,
  config?: { programAddress?: TProgramAddress }
): ApproveFoundReportInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountConfig,
  TAccountLostPost,
  TAccountFoundReport,
  TAccountFinder
> {
  // Program address.
  const programAddress = config?.programAddress ?? FINDARE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    lostPost: { value: input.lostPost ?? null, isWritable: true },
    foundReport: { value: input.foundReport ?? null, isWritable: true },
    finder: { value: input.finder ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.lostPost),
      getAccountMeta(accounts.foundReport),
      getAccountMeta(accounts.finder),
    ],
    data: getApproveFoundReportInstructionDataEncoder().encode({}),
    programAddress,
  } as ApproveFoundReportInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig,
    TAccountLostPost,
    TAccountFoundReport,
    TAccountFinder
  >);
}

export type ParsedApproveFoundReportInstruction<
  TProgram extends string = typeof FINDARE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    config: TAccountMetas[1];
    lostPost: TAccountMetas[2];
    foundReport: TAccountMetas[3];
    finder: TAccountMetas[4];
  };
  data: ApproveFoundReportInstructionData;
};

export function parseApproveFoundReportInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedApproveFoundReportInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      config: getNextAccount(),
      lostPost: getNextAccount(),
      foundReport: getNextAccount(),
      finder: getNextAccount(),
    },
    data: getApproveFoundReportInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
